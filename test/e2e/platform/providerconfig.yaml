apiVersion: external-secrets.services.openmcp.cloud/v1alpha1
kind: ProviderConfig
metadata:
  name: externalsecretsoperator
spec:
  pollInterval: 1m
  # chartURL: "oci://somewhere-else"
  chartPullSecret: test
  helmValues:
    namespaceOverride: "eso-system"
    imagePullSecrets:
      - name: test
    # image:
    #   repository: "somewhere-else"
